name: Simple Local

on: 
  push:

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v3

      - name: Diga oi
        run: echo "Hello World!"

